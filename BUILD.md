# Build

Open this folder in Android Studio. Android Studio will create/use the Gradle wrapper during project setup.

Then run:

`./gradlew assembleRelease`

The GitHub Actions workflow builds the release APK when you push a `v*` tag.
